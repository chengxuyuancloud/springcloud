server:
  port: 5001

spring:
  application:
    name: SpringCloud-Config
  cloud:
    config:
      server:
        git: # 远程库的git地址
          uri: https://github.com/chamberlaincloud/SpringCloudConfig.git